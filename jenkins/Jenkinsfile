pipeline {
    agent none
    stages {
        stage('Build') {
             agent {
                docker {
                    image 'python:3-alpine'
                }
            }
            steps {          
                withEnv(["HOME=${env.WORKSPACE}"]){                  
                    sh 'python -m py_compile StockFlow/manage.py'
                }
            }
        }
        stage('Test') {
            agent {
                docker {
                    image 'django'
                    image 'python:3.7'
                    image 'tadeorubio/pyodbc-msodbcsql17'
                }
            } 
            
             steps{
                sh 'pip install -r requirements.txt'
                withEnv(["HOME=${env.WORKSPACE}"]){  
                     dir('StockFlow'){                    
                        sh 'python -m coverage run --source="." manage.py test'
                        sh 'python -m coverage report'
                        sh 'python manage.py check'
                }
                }
             }
        }
    }
}
