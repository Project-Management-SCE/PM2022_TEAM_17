pipeline {
    agent none
    stages {
        stage('Build') {
             agent {
                docker {
                    image 'python:3-alpine'
                }
            }
            steps {
                sh 'python -m py_compile StockFlow/manage.py'
            }
        }
        stage('Test') {
            agent {
                docker {
                    image 'django'
                }
            } 
            
            steps {
                sh 'python -m py_compile StockFlow/AgentSignUp/tests/test_AgentSignUp.py  --junit-xml test-reports/results.xml'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }
        }
    }
}
